# Puerto del servidor (puede omitirse si usas Railway por defecto)
server.port=8080

# Configuración de conexión a la base de datos (Railway variables)
spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}

# Configuración de Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Opcional: desactivar validación al inicio (para evitar errores si falta la base)
spring.sql.init.mode=never
